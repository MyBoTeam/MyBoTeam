{
  "name": "@myboteam/whatsapp-mcp",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "check": "biome check --write . && tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.0.17",
    "@types/node": "^25.9.1"
  }
}
