# Contracts: Eve Materializer

No external API contracts — the materializer is an internal module consumed by the daemon. Its public API is the TypeScript `materialize()` and `dematerialize()` functions.
